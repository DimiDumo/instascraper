{
  "name": "instascraper-cloud",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrations:apply:local": "wrangler d1 migrations apply instascraper-db --local",
    "db:migrations:apply:remote": "wrangler d1 migrations apply instascraper-db --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5",
    "wrangler": "^4.51.0"
  }
}
